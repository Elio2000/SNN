[project]
name = "snn-eprop"
version = "0.1.0"
description = "SNN e-prop experiments for CIM-oriented on-chip learning exploration."
readme = "AGENTS.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "torch>=2.11.0",
    "numpy>=1.26",
    "matplotlib>=3.8",
    "librosa>=0.10",
    "scipy>=1.11",
]

[tool.uv]
package = false
